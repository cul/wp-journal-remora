jQuery(function($) {

	// Ensure dropdowns will toggle
	$('.dropdown-toggle').dropdown();

	// Requires jquery.placeholder.min.js
	// Add support for placeholder attribute in older browsers.
	$('input[placeholder]').placeholder();

});
